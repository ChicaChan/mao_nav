# 博客管理后台本地使用指南

## 📝 说明

博客管理后台仅在本地环境使用，不部署到线上。这样做的好处：

- ✅ 安全性高（管理后台不对外开放）
- ✅ 无需额外服务器成本
- ✅ 简单易用

## 🚀 使用步骤

### 1. 启动后端服务

在项目根目录或 `blog/` 目录下运行：

```bash
# 方式 1：在 blog 目录下
cd blog
npm run server

# 方式 2：在项目根目录下
cd F:\项目\Github项目\mao_nav\blog
npm run server
```

后端服务会在 `http://localhost:3001` 上运行。

### 2. 启动博客开发服务器

在另一个终端窗口中运行：

```bash
# 方式 1：在 blog 目录下
cd blog
npm run dev

# 方式 2：在项目根目录下
cd F:\项目\Github项目\mao_nav\blog
npm run dev
```

博客开发服务器会在 `http://localhost:4321` 上运行。

### 3. 访问管理后台

打开浏览器访问：

```
http://localhost:4321/admin/
```

**注意**：必须带上末尾的斜杠 `/`

### 4. 管理文章

在管理后台中，你可以：

- ✏️ 创建新文章
- 📝 编辑现有文章
- 🗑️ 删除文章

所有操作都会直接修改 `blog/src/content/posts/` 目录下的 Markdown 文件。

### 5. 提交更改到 GitHub

编辑完成后，提交更改：

```bash
# 在项目根目录下
git add .
git commit -m "feat: 添加/更新博客文章"
git push
```

Cloudflare Pages 会自动检测到推送并重新部署网站。

## 🔧 环境要求

- Node.js >= 18.0.0
- npm >= 8.0.0

## 📂 文件结构

```
blog/
├── server.js                          # Express 后端服务
├── src/
│   ├── pages/
│   │   └── admin.astro               # 管理后台页面（仅本地）
│   ├── components/
│   │   └── admin/
│   │       └── BlogAdmin.svelte      # 管理后台组件
│   ├── utils/
│   │   ├── blogAPI.js                # 后端 API 调用
│   │   └── markdownParser.js         # Markdown 解析工具
│   └── content/
│       └── posts/                    # 博客文章目录
│           ├── article-1.md
│           ├── article-2.md
│           └── ...
```

## ⚠️ 注意事项

1. **管理后台不会部署到线上** - 只在本地开发环境使用
2. **后端服务必须运行** - 否则管理后台无法连接
3. **文章直接保存到本地** - 不会自动提交到 Git
4. **手动提交更改** - 编辑完成后需要手动 `git commit` 和 `git push`

## 🐛 常见问题

### Q: 管理后台显示"无法连接到后端服务"

**A**: 确保后端服务已启动：
```bash
cd blog
npm run server
```

### Q: 端口被占用怎么办？

**A**:
- 后端服务端口 3001 被占用：修改 `blog/server.js` 中的 `PORT` 变量
- 开发服务器端口被占用：Astro 会自动尝试其他端口（4322, 4323 等）

### Q: 如何查看已发布的文章？

**A**: 访问线上网站：`https://mao-nav-d0e.pages.dev`

## 📞 技术支持

如有问题，请查看：
- [Astro 文档](https://docs.astro.build/)
- [Express 文档](https://expressjs.com/)
- 项目根目录的 `DEPLOYMENT_GUIDE.md`
